import update from "immutability-helper";

const actions = {
  login: (email, password) => async ({ getState, setState }) => {},
  join: (name, email, password) => async ({ getState, setState }) => {}
};

export default actions;
